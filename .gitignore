# Data (too large for GitHub — download via scripts)
data/
download.log

# Model checkpoints (upload to HuggingFace instead)
checkpoints/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz

# Archives
*.tar.gz

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Firebase (not part of research)
firebase-debug.log

# GPD internal state
.gpd/state.json
.gpd/state.json.bak
.gpd/observability/
.gpd/traces/

# Claude Code / GPD runtime install (not project artifacts)
.claude/
.mcp.json
