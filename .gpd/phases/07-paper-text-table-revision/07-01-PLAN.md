---
phase: 07-paper-text-table-revision
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - paper/main.tex
  - paper/tables/table_cw.tex
  - paper/tables/table_overall.tex
interactive: false

conventions:
  units: "SI (Hz, s, strain dimensionless)"
  metric_primary: "macro-F1 (sklearn f1_score average='macro')"
  bootstrap: "10K resamples, seed=42, 95% CI, percentile method"
  significance: "p < 0.05 paired bootstrap"
  rounding: "3 decimal places for F1/accuracy, 1 decimal for percentages"

approximations:
  - name: "duty cycle as CW sensitivity proxy"
    parameter: "noise PSD approximately stationary across observation"
    validity: "valid when noise floor comparable across retained/vetoed segments"
    breaks_when: "ViT and CNN veto different time periods with significantly different noise characteristics"
    check: "Stated explicitly in paper per REF-005"

estimated_execution:
  total_minutes: 40
  breakdown:
    - task: 1
      minutes: 25
      note: "8 text revisions across 6 paper sections + abstract + conclusion"
    - task: 2
      minutes: 15
      note: "CW table restructure + O4 rounding fix"

contract:
  scope:
    question: "Does the revised paper honestly address all 9 referee issues with consistent framing, corrected tables, and integrated Phase 6 quantitative results?"
  claims:
    - id: claim-class-dependent-reframe
      statement: "All 4 instances of 'class-morphology-dependent' replaced with 'class-dependent'; Power_Line/Chirp examples reframed as illustrative hypotheses not established mechanisms"
      deliverables: [deliv-revised-manuscript]
      acceptance_tests: [test-no-morphology-dependent, test-hypothesis-language]
      references: [ref-gravity-spy]
    - id: claim-rare-reframe
      statement: "Rare-class narrative throughout paper reframed from 'underperforms' to 'insufficient evidence' with Phase 6 power analysis numbers integrated"
      deliverables: [deliv-revised-manuscript]
      acceptance_tests: [test-rare-reframe-complete, test-power-numbers-present]
      references: [ref-gravity-spy]
    - id: claim-cw-table-fixed
      statement: "CW Table 3 has separate F1 Advantage and DC Advantage columns with internally consistent sign conventions"
      deliverables: [deliv-revised-cw-table]
      acceptance_tests: [test-cw-columns-consistent]
      references: []
    - id: claim-minor-issues-resolved
      statement: "All 5 minor referee issues resolved: CW proxy validity (REF-005), Davis citation (REF-006), seed limitation (REF-007), O4 rounding (REF-008), asymmetric degradation discussion (REF-009)"
      deliverables: [deliv-revised-manuscript, deliv-revised-overall-table]
      acceptance_tests: [test-five-minor-resolved]
      references: [ref-davis-2021]
    - id: claim-phase6-integrated
      statement: "Random-split ablation (95.4% accuracy) and power analysis (aggregate power 0.20) from Phase 6 incorporated into paper text"
      deliverables: [deliv-revised-manuscript]
      acceptance_tests: [test-ablation-in-paper, test-power-in-paper]
      references: [ref-gravity-spy]
  deliverables:
    - id: deliv-revised-manuscript
      kind: report
      path: "paper/main.tex"
      description: "Revised CQG manuscript addressing all 9 referee issues"
      must_contain:
        - "class-dependent (not class-morphology-dependent)"
        - "insufficient evidence (not underperforms) for rare classes"
        - "random-split accuracy 95.4%"
        - "power analysis aggregate power 0.20"
        - "Davis et al. citation in Sec 2.4"
        - "single training run limitation statement"
        - "CW proxy validity statement"
        - "asymmetric O4 degradation discussion"
    - id: deliv-revised-cw-table
      kind: table
      path: "paper/tables/table_cw.tex"
      description: "CW Table 3 with separate F1 Advantage and DC Advantage columns"
      must_contain:
        - "F1 Adv column"
        - "DC Adv column"
    - id: deliv-revised-overall-table
      kind: table
      path: "paper/tables/table_overall.tex"
      description: "Table 1 with consistent O4 rounding"
      must_contain:
        - "O4 ViT macro-F1 rounded consistently to 3dp"
  acceptance_tests:
    - id: test-no-morphology-dependent
      subject: claim-class-dependent-reframe
      kind: existence
      procedure: "grep -c 'morphology-dependent' paper/main.tex must return 0"
      pass_condition: "Zero occurrences of 'morphology-dependent' in the manuscript"
      evidence_required: [deliv-revised-manuscript]
    - id: test-hypothesis-language
      subject: claim-class-dependent-reframe
      kind: human_review
      procedure: "Verify Power_Line and Chirp examples use hedged language ('suggests', 'consistent with', 'one hypothesis') rather than causal claims"
      pass_condition: "No unhedged causal claims about morphological complexity driving architecture preference"
      evidence_required: [deliv-revised-manuscript]
    - id: test-rare-reframe-complete
      subject: claim-rare-reframe
      kind: existence
      procedure: "grep -c 'underperforms' paper/main.tex must return 0 in the context of rare-class ViT results"
      pass_condition: "No instance of 'underperforms' used to describe ViT rare-class results; replaced with 'insufficient evidence' or equivalent"
      evidence_required: [deliv-revised-manuscript]
    - id: test-power-numbers-present
      subject: claim-rare-reframe
      kind: existence
      procedure: "Verify paper contains: aggregate power 0.20, Chirp n=7 power 0.035, Wandering_Line n=6 power 0.017"
      pass_condition: "At least aggregate power and 2 individual class power values appear in paper text"
      evidence_required: [deliv-revised-manuscript]
    - id: test-cw-columns-consistent
      subject: claim-cw-table-fixed
      kind: consistency
      procedure: "For each row in CW Table 3: F1 Adv column matches sign of delta-F1, DC Adv column matches sign of delta_DC. Violin_Mode DC Adv = CNN (delta_DC negative). Power_Line DC Adv = CNN (delta_DC negative). Low_Frequency_Burst DC Adv = ViT (delta_DC positive)."
      pass_condition: "All rows internally consistent for both advantage columns"
      evidence_required: [deliv-revised-cw-table]
    - id: test-five-minor-resolved
      subject: claim-minor-issues-resolved
      kind: existence
      procedure: "Verify presence of: (1) CW proxy validity sentence in Sec 2.4, (2) Davis et al. citation in Sec 2.4, (3) single-run limitation statement, (4) O4 rounding consistency, (5) asymmetric degradation discussion in Sec 4.2"
      pass_condition: "All 5 items present"
      evidence_required: [deliv-revised-manuscript, deliv-revised-overall-table]
    - id: test-ablation-in-paper
      subject: claim-phase6-integrated
      kind: benchmark
      procedure: "Verify random-split accuracy 0.9544 or 95.4% appears in paper with comparison to published 95-99% range"
      pass_condition: "Random-split ablation result cited with Gravity Spy benchmark comparison"
      evidence_required: [deliv-revised-manuscript]
    - id: test-power-in-paper
      subject: claim-phase6-integrated
      kind: existence
      procedure: "Verify power analysis results appear in rare-class discussion"
      pass_condition: "Power analysis numbers present in Sec 3.1 or Sec 4.1"
      evidence_required: [deliv-revised-manuscript]
  references:
    - id: ref-gravity-spy
      kind: paper
      locator: "Zevin et al., Classical and Quantum Gravity, 2017"
      role: benchmark
      why_it_matters: "Random-split ablation accuracy compared to published 95-99% benchmark range"
      must_surface: true
      required_actions: [cite]
      applies_to: [claim-class-dependent-reframe, claim-rare-reframe, claim-phase6-integrated]
    - id: ref-davis-2021
      kind: paper
      locator: "Davis et al., 2021"
      role: method
      why_it_matters: "CW veto efficiency framework citation required in Sec 2.4"
      must_surface: true
      required_actions: [cite]
      applies_to: [claim-minor-issues-resolved]
  forbidden_proxies:
    - id: fp-keep-morphology-dependent
      subject: claim-class-dependent-reframe
      proxy: "Keeping 'morphology-dependent' anywhere in the paper without a quantitative morphological complexity metric"
      reason: "Referee Issue 1 explicitly flags this as an unsupported causal claim; no morphological complexity metric exists in the paper"
    - id: fp-mixed-advantage-column
      subject: claim-cw-table-fixed
      proxy: "Single 'Advantage' column mixing F1-based and DC-based judgments"
      reason: "Referee Issue 2 shows the mixed column contradicts delta_DC signs for 3 rows, undermining trust in the CW analysis"
    - id: fp-rare-underperforms-without-power
      subject: claim-rare-reframe
      proxy: "Claiming ViT 'underperforms' on rare classes without citing the power analysis showing insufficient evidence"
      reason: "Referee Issue 4 demonstrates the rare-class comparison is severely underpowered (aggregate power 0.20); 'underperforms' overstates the evidence"
    - id: fp-overall-accuracy
      subject: claim-phase6-integrated
      proxy: "Citing overall accuracy improvement as evidence of rare-class improvement"
      reason: "Project convention lock: macro-F1 is primary metric; overall accuracy is a forbidden proxy per project contract"
  uncertainty_markers:
    weakest_anchors:
      - "Power analysis uses single-seed ablation; multi-seed would strengthen the claim"
      - "CW proxy validity statement is an assertion, not a validated condition"
    disconfirming_observations:
      - "If a quantitative morphological complexity metric were computed and showed correlation with delta-F1, the 'class-dependent' reframing would be too conservative"
      - "If multi-seed training showed rare-class results are seed-dependent, the 'insufficient evidence' framing may understate the problem"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []
---

<objective>
Revise the CQG manuscript to address all 9 referee issues (4 major, 5 minor) by making targeted text edits, restructuring the CW table, fixing rounding inconsistencies, and integrating Phase 6 quantitative results (random-split ablation, power analysis).

Purpose: Produce a submission-ready manuscript that honestly reframes claims to match the evidence -- "class-dependent" instead of "class-morphology-dependent", "insufficient evidence" instead of "underperforms" -- while preserving the paper's core empirical contribution.
Output: Revised paper/main.tex, paper/tables/table_cw.tex, paper/tables/table_overall.tex with all 9 REF issues resolved.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REFEREE-REPORT.md
@.gpd/phases/06-computation-statistical-analysis/06-01-SUMMARY.md
@paper/main.tex
@paper/tables/table_cw.tex
@paper/tables/table_overall.tex
@paper/tables/table_perclass.tex
@results/06-computation-statistical-analysis/ablation_summary.txt
@results/06-computation-statistical-analysis/random_split_ablation.json
@results/06-computation-statistical-analysis/power_analysis.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Text revisions -- reframing, new content, and Phase 6 integration</name>
  <files>paper/main.tex</files>
  <action>
    Make the following targeted edits to paper/main.tex. For each edit, the referee issue ID is noted.

    **REF-001: Replace "class-morphology-dependent" with "class-dependent" in 4 locations:**

    1. Abstract (line ~109): Change "class-morphology-dependent" to "class-dependent". Remove "rather than a monotonic function of training set size" (the O4 Spearman test belongs in Results, not Abstract). Replace with a concise statement that architecture preference varies by class.

    2. Introduction enumerated item 1 (line ~176): Change "class-morphology-dependent" to "class-dependent". Reword "ViT improves F1 on morphologically complex classes but underperforms on rare, data-scarce categories" to use hedged language: "ViT improves F1 on several common classes but shows no significant improvement on rare categories (power analysis confirms insufficient statistical evidence for rare-class comparison)."

    3. Section 3.3 (line ~383-386): Replace the paragraph beginning "The pattern is better described as class-morphology-dependent" with: "The pattern is better described as \emph{class-dependent} than \emph{sample-size-dependent}: specific classes consistently favor one architecture across O3 and O4 (e.g., Power\_Line favors ViT, Chirp favors CNN), but we lack a quantitative morphological complexity metric to test whether this preference correlates with glitch structure. We present the Power\_Line and Chirp examples as illustrative hypotheses, not established mechanisms."

    4. Conclusion (line ~630-633): Change "morphologically complex classes that benefit from global self-attention" to "classes such as Power\_Line, where global self-attention may capture multi-frequency harmonic structure". Change "rare and morphologically simple classes" to "rare classes where limited training data appears insufficient for learning effective attention patterns". Remove the unsupported causal claim.

    **REF-003: Integrate random-split ablation (Phase 6) into paper:**

    Add a new paragraph in Section 3.2 (Overall Comparison, after the aggregate metrics paragraph around line ~349) or a new subsection 3.2.1:
    "To assess whether the gap between our CNN test accuracy (91.8\%) and published Gravity Spy benchmarks (95--99\%, \citealt{Zevin2017}) reflects a pipeline deficiency or the temporal split methodology, we retrained the CNN with identical hyperparameters on a stratified random split of the same O3 data. The random-split CNN achieves 95.4\% accuracy [95\% CI: 95.3\%, 95.6\%], falling within the published range and confirming that the temporal split---not a systematic training issue---explains the accuracy gap. Macro-F1 also increases from 0.679 to 0.751, further supporting this interpretation."

    **REF-004 / STAT-02: Reframe rare-class narrative with power analysis:**

    a) In the abstract (line ~106-107): Replace "but underperforms on rare classes" with "but the rare-class comparison is statistically underpowered (aggregate power = 0.20), yielding insufficient evidence to determine whether ViT improves or degrades rare-class performance."

    b) In per-class results discussion (around line ~337-341, Chirp bullet): After the Chirp bullet, add: "However, with only 7 test samples for Chirp and 6 for Wandering\_Line, these per-class comparisons are individually unreliable. A simulation-based power analysis (10\,000 iterations) finds that all 8 rare classes are underpowered to detect the observed $-0.062$ macro-F1 difference at $\alpha = 0.05$: power ranges from 0.017 (Wandering\_Line) to 0.310 (1080Lines), with aggregate rare-class power of 0.20. The rare-class macro-F1 difference should therefore be interpreted as \emph{insufficient statistical evidence} rather than a definitive finding of ViT underperformance."

    c) In Section 3.2 (line ~349): Where rare-class macro-F1 is reported as "lower", change language from "underperforms" or "lower" to "not significantly different, given the severe underpowering of the rare-class comparison (aggregate power = 0.20)."

    d) In Discussion Section 4.1 (line ~496-500): After Chirp discussion, add: "We emphasize that this interpretation is speculative: the rare-class test sets are too small to draw reliable per-class conclusions (Chirp: power = 0.035 at the observed effect, Wandering\_Line: power = 0.017)."

    e) In Conclusion (line ~640): Replace "The negative result on rare classes is itself the most actionable finding" with "The rare-class comparison is underpowered (aggregate power = 0.20), so the apparent regression should be treated as insufficient evidence rather than a confirmed finding. This statistical limitation is itself actionable: it directs future effort toward larger rare-class test sets alongside data augmentation and few-shot learning."

    **REF-005: Add CW proxy validity statement in Sec 2.4:**

    After line ~291 ("to ensure a fair comparison"), add: "This sample-count--based duty cycle is a valid proxy for CW search sensitivity under the assumption that the noise power spectral density is approximately stationary across the observation period, so that livetime fraction is proportional to integrated sensitivity~\citep{Davis2021}."

    **REF-006: Add Davis et al. citation in Sec 2.4:**

    In the CW veto analysis paragraph (line ~284-286), after "translates to data-quality vetoes that remove contaminated time segments", add: "following the veto efficiency analysis framework of \citet{Davis2021}."

    **REF-007: Add single-run limitation statement:**

    In the Limitations section (after item 2 or as a new item 3, around line ~582), add:
    "\item \textbf{Single training run.} Both models are trained once with a fixed random seed. Per-class F1 differences, particularly for rare classes with few samples, may not be robust to training stochasticity. A multi-seed assessment (3--5 runs) would strengthen the per-class conclusions but is beyond the scope of this revision."

    **REF-009: Add asymmetric O4 degradation discussion in Sec 4.2:**

    After line ~515 ("not universal laws"), add a new paragraph:
    "The asymmetric degradation rates---ViT loses 7.4\% relative macro-F1 on O4 compared to CNN's 1.7\%---provide additional evidence for the role of architectural inductive bias in generalization. CNN's convolutional filters encode translation equivariance, a prior that remains valid across observing runs regardless of the specific noise environment. ViT's self-attention, by contrast, can overfit to O3-specific spectral correlations that do not persist in O4. This interpretation is consistent with the broader machine learning literature on Transformer generalization under distribution shift."
  </action>
  <verify>
    1. grep -c "morphology-dependent" paper/main.tex returns 0
    2. grep -c "underperforms" paper/main.tex returns 0 in the context of ViT rare-class results (may appear in quoted referee language if kept, but NOT in the paper's own claims)
    3. grep "insufficient evidence\|insufficient statistical evidence\|underpowered" paper/main.tex returns hits in abstract, Sec 3.1, Sec 3.2, Sec 4.1, and conclusion
    4. grep "95.4\|0.9544\|random.split" paper/main.tex returns hits in Sec 3.2 (ablation paragraph)
    5. grep "power.*0.20\|aggregate.*power\|power.*analysis" paper/main.tex returns hits
    6. grep "Davis2021" paper/main.tex returns hits in Sec 2.4 (not just bibliography)
    7. grep "Single training run\|single.*seed\|training stochasticity" paper/main.tex returns hits in Limitations
    8. grep "asymmetric degradation\|7.4.*1.7\|inductive bias.*generalization" paper/main.tex returns hits in Sec 4.2
    9. grep "approximately stationary\|noise power spectral density" paper/main.tex returns hits in Sec 2.4
    10. LaTeX compiles without errors: pdflatex paper/main.tex (if available) or at minimum no unmatched braces, no undefined citations
    11. Count total lines changed: should be additions/modifications in ~8-10 locations, no deletions of existing verified numerical results
  </verify>
  <done>All 7 text-based referee issues (REF-001, REF-003, REF-004/STAT-02, REF-005, REF-006, REF-007, REF-009) resolved in paper/main.tex. Zero instances of "morphology-dependent" remain. Rare-class narrative consistently uses "insufficient evidence" framing with power analysis numbers. Phase 6 ablation results integrated. All new text uses hedged language for speculative interpretations.</done>
</task>

<task type="auto">
  <name>Task 2: Table corrections -- CW dual-advantage columns and O4 rounding</name>
  <files>paper/tables/table_cw.tex, paper/tables/table_overall.tex</files>
  <action>
    **REF-002: Restructure CW Table 3 with separate F1 and DC advantage columns:**

    Replace the current single "Advantage" column with two columns: "F1 Adv" and "DC Adv".

    The F1 Advantage is based on which model has higher per-class F1 on O4 data. The DC Advantage is based on the sign of delta_DC (positive = ViT retains more livetime = ViT advantage; negative = CNN retains more livetime = CNN advantage).

    Correct mapping (from the existing data):
    - Low_Frequency_Lines: F1 Adv = CNN, DC Adv = CNN (delta_DC = -0.0264)
    - Scattered_Light: F1 Adv = CNN, DC Adv = CNN (delta_DC = -0.0112)
    - Violin_Mode: F1 Adv = ViT, DC Adv = CNN (delta_DC = -0.0043) -- NOTE: F1 favors ViT but DC favors CNN
    - Power_Line: F1 Adv = ViT, DC Adv = CNN (delta_DC = -0.0144) -- NOTE: F1 favors ViT but DC favors CNN
    - 1080Lines: F1 Adv = CNN, DC Adv = CNN (delta_DC = -0.0007)
    - Low_Frequency_Burst: F1 Adv = CNN, DC Adv = ViT (delta_DC = +0.0057) -- NOTE: DC favors ViT but F1 favors CNN
    - Whistle: F1 Adv = CNN, DC Adv = CNN (delta_DC = +0.0004, but CI includes zero, so effectively tie; label CNN since point estimate slightly negative after bootstrap... actually delta_DC = +0.0004 means ViT retains slightly more, but CI spans zero) -- Use "---" or "ViT" for DC Adv since point estimate is positive

    Update the table format from 6 columns to 7:
    ```
    Class & CW impact & N_O4 & ViT DC & CNN DC & delta_DC & F1 Adv & DC Adv
    ```

    Update the table caption to explain both columns:
    "F1 Adv indicates which model achieves higher per-class F1 on O4 data; DC Adv indicates which model retains more observation time (based on sign of $\Delta_\mathrm{DC}$). These can disagree when a model classifies a class more accurately but with lower confidence scores, leading to more aggressive vetoing."

    Also update the in-text reference to Table 3 (line ~462-464 in main.tex): Change "two favor ViT (Power_Line, Violin_Mode) and five favor CNN" to "two favor ViT on F1 (Power\_Line, Violin\_Mode) while CNN retains more observation time for 6 of 7 classes. The disagreement between F1 advantage and duty-cycle advantage for Power\_Line and Violin\_Mode highlights that better classification accuracy does not automatically translate to better CW data quality at a given operating point."

    **REF-008: Fix O4 macro-F1 rounding in Table 1:**

    In paper/tables/table_overall.tex, the O4 row shows ViT as 0.669. The full-precision value is 0.6695. Rounded to 3 decimal places this is 0.670 (since the 4th digit is 5, round up). Alternatively, if the convention is truncation, 0.669 is correct.

    Check: CNN O4 = 0.6674 rounds to 0.667 (correct in table). ViT O4 = 0.6695 rounds to 0.670 (table shows 0.669 -- inconsistent with standard rounding).

    Fix: Change ViT O4 macro-F1 from 0.669 to 0.670 in the table. If the table uses LaTeX macros, update the macro; if hardcoded, update the value directly. Ensure this matches the text macro \vitMacroFOfour{} which should also be 0.670.

    Verify the CI bounds for O4 are also consistently rounded:
    - CNN O4: 0.6674 [0.6567, 0.6765] -> 0.667 [0.657, 0.677] (check table)
    - ViT O4: 0.6695 [0.6555, 0.6816] -> 0.670 [0.656, 0.682] (check table, fix if needed)
  </action>
  <verify>
    1. CW table has exactly 7 data columns (Class, CW impact, N_O4, ViT DC, CNN DC, delta_DC, F1 Adv, DC Adv) -- that is 8 columns total
    2. F1 Adv and DC Adv are internally consistent with the data:
       - Power_Line: F1 Adv = ViT, DC Adv = CNN (delta_DC is negative)
       - Violin_Mode: F1 Adv = ViT, DC Adv = CNN (delta_DC is negative)
       - Low_Frequency_Burst: DC Adv = ViT (delta_DC is positive)
    3. No row has a DC Adv that contradicts the sign of delta_DC
    4. Table caption explains both columns and notes they can disagree
    5. O4 ViT macro-F1 in table_overall.tex reads 0.670 (not 0.669)
    6. O4 CI bounds rounded consistently to 3 decimal places
    7. The in-text reference to Table 3 in main.tex is updated to distinguish F1 vs DC advantage
    8. LaTeX compiles without errors (no column count mismatches, no missing & separators)
  </verify>
  <done>CW Table 3 restructured with separate F1 Adv and DC Adv columns; all rows internally consistent with data; caption explains disagreement mechanism. O4 macro-F1 rounding fixed to 0.670 for ViT. Both tables compile correctly. REF-002 and REF-008 fully resolved.</done>
</task>

</tasks>

<verification>
Post-completion checks across all edits:
- Zero instances of "morphology-dependent" in paper/main.tex (REF-001)
- CW Table 3 has dual advantage columns with no sign contradictions (REF-002)
- Random-split ablation (95.4%) cited with Gravity Spy benchmark comparison (REF-003)
- Rare-class narrative uses "insufficient evidence" throughout, power numbers present (REF-004)
- CW proxy validity statement in Sec 2.4 (REF-005)
- Davis et al. cited in Sec 2.4 methods paragraph (REF-006)
- Single-run limitation statement in Limitations section (REF-007)
- O4 ViT macro-F1 rounds to 0.670 consistently (REF-008)
- Asymmetric degradation discussion in Sec 4.2 (REF-009)
- No existing verified numerical values altered (only framing language changed)
- No forbidden proxy: no "morphology-dependent" without metric, no mixed advantage column, no "underperforms" without power context, no overall accuracy as primary metric
</verification>

<success_criteria>
All 9 referee issues (REF-001 through REF-009) resolved in paper files. "class-morphology-dependent" replaced with "class-dependent" in all 4 locations. CW Table 3 has separate F1 and DC advantage columns that are internally consistent. Rare-class narrative reframed with power analysis numbers from Phase 6. Random-split ablation from Phase 6 integrated. All 5 minor issues addressed. No numerical values altered except O4 rounding fix. Paper compiles without errors.
</success_criteria>

<output>
After completion, create `.gpd/phases/07-paper-text-table-revision/07-01-SUMMARY.md`
</output>
