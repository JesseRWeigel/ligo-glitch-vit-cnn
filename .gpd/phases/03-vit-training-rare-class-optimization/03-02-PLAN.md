---
phase: 03-vit-training-rare-class-optimization
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/evaluation/paired_bootstrap.py
  - scripts/12_evaluate_vit.py
  - results/03-vit-rare-class/metrics.json
  - results/03-vit-rare-class/per_class_f1.csv
  - results/03-vit-rare-class/predictions.npz
  - results/03-vit-rare-class/comparison_table.csv
  - results/03-vit-rare-class/paired_bootstrap_results.json
  - results/03-vit-rare-class/statistical_summary.md
  - figures/vit_confusion_matrix.png
  - figures/comparison_confusion_matrices.png
  - figures/comparison_per_class_f1.png
interactive: false

conventions:
  units: "SI (strain dimensionless, frequency Hz, time GPS seconds)"
  input_normalization: "ImageNet: mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]"
  image_size: "224x224 RGB"
  primary_metric: "Macro-averaged F1 (overall and rare-class)"
  loss: "Focal loss: FL(p_t) = -alpha_t * (1 - p_t)^gamma * log(p_t), gamma=2.0, sqrt-inverse alpha, label_smoothing=0.1"
  rare_class_threshold: "< 200 training samples (4 classes: Chirp, Wandering_Line, Helix, Light_Modulation)"
  bootstrap: ">= 10,000 resamples, paired on same test indices, p < 0.05 one-sided"
  statistical_test: "Paired bootstrap on difference in rare-class macro-F1; H0: ViT <= CNN"

approximations:
  - name: "Paired percentile bootstrap CI"
    parameter: "N_test = 48,845 overall; rare classes have N_test = 6, 7, 14, 66"
    validity: "Stable for overall macro-F1 (large N); wide CIs expected for individual rare classes"
    breaks_when: "Per-class CIs for N_test < 10 classes span 0-1 (Wandering_Line, Chirp)"
    check: "Report CI widths; flag if rare-class macro-F1 CI width > 0.4"

estimated_execution:
  total_minutes: 30
  breakdown:
    - task: 1
      minutes: 15
      note: "Re-evaluate CNN + evaluate ViT on test set, paired bootstrap, metric computation"
    - task: 2
      minutes: 15
      note: "Comparison table, confusion matrices, statistical summary"

contract:
  scope:
    question: "Does the ViT-B/16 achieve statistically significant improvement in rare-class macro-F1 over the CNN baseline on the held-out O3 temporal test set?"
  claims:
    - id: claim-rare-improvement
      statement: "A ViT-B/16 classifier achieves higher F1/recall on rare glitch classes than the CNN baseline when trained on O3 LIGO spectrogram data"
      deliverables: [deliv-comparison-table, deliv-confusion-matrix, deliv-vit-metrics]
      acceptance_tests: [test-rare-f1-improvement, test-metric-consistency, test-paired-bootstrap-valid]
      references: [ref-cnn-baseline, ref-gravity-spy]
  deliverables:
    - id: deliv-comparison-table
      kind: table
      path: "results/03-vit-rare-class/comparison_table.csv"
      description: "Per-class F1 and recall comparison table: ViT vs CNN for all 23 classes"
      must_contain: ["per-class F1 for both models", "per-class recall for both models", "class sample counts (n_train, n_test)", "bootstrap 95% CIs for both models", "F1 difference (ViT minus CNN) per class", "statistical significance flag for rare-class macro-F1"]
    - id: deliv-confusion-matrix
      kind: figure
      path: "figures/comparison_confusion_matrices.png"
      description: "Side-by-side confusion matrices for ViT and CNN with rare-class highlighting"
      must_contain: ["ViT confusion matrix (left)", "CNN confusion matrix (right)", "rare classes highlighted with colored border", "row-normalized values", "raw count annotations"]
    - id: deliv-vit-metrics
      kind: data
      path: "results/03-vit-rare-class/metrics.json"
      description: "ViT test set metrics with bootstrap CIs, mirroring Phase 2 metrics.json structure"
      must_contain: ["macro_f1 with CI", "rare_class_macro_f1 with CI", "overall_accuracy (labeled SANITY CHECK)", "per_class_f1 dict"]
    - id: deliv-paired-bootstrap
      kind: data
      path: "results/03-vit-rare-class/paired_bootstrap_results.json"
      description: "Paired bootstrap results for ViT vs CNN rare-class macro-F1 difference"
      must_contain: ["point_estimate_difference", "ci_lower", "ci_upper", "p_value", "n_resamples", "n_test_samples", "test_manifest_hash"]
    - id: deliv-statistical-summary
      kind: note
      path: "results/03-vit-rare-class/statistical_summary.md"
      description: "Human-readable summary of statistical comparison with verdict"
      must_contain: ["CNN rare-class macro-F1 value", "ViT rare-class macro-F1 value", "difference with CI", "p-value", "verdict (significant/not significant)", "backtracking trigger assessment"]
  references:
    - id: ref-cnn-baseline
      kind: prior_artifact
      locator: "results/02-cnn-baseline/metrics.json AND checkpoints/02-cnn-baseline/best_model.pt"
      aliases: ["Phase 2 CNN baseline"]
      role: benchmark
      why_it_matters: "Provides both the metric floor (rare-class macro-F1 = 0.3028) and the CNN checkpoint for re-evaluation to obtain per-sample predictions for paired bootstrap"
      applies_to: [claim-rare-improvement]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-gravity-spy
      kind: paper
      locator: "Zevin et al. 2017; Gravity Spy taxonomy (23 classes)"
      aliases: ["Gravity Spy"]
      role: definition
      why_it_matters: "Defines the 23-class taxonomy; class labels must match throughout"
      applies_to: [claim-rare-improvement]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [read, cite]
  acceptance_tests:
    - id: test-rare-f1-improvement
      subject: claim-rare-improvement
      kind: benchmark
      procedure: "Compare ViT rare-class macro-F1 against CNN rare-class macro-F1 (0.3028) on the same held-out O3 temporal test set (N=48,845). Run paired bootstrap (>= 10,000 resamples) on the difference. Report one-sided p-value for H0: ViT rare-class macro-F1 <= CNN rare-class macro-F1."
      pass_condition: "ViT rare-class macro-F1 > CNN rare-class macro-F1 AND paired bootstrap p-value < 0.05"
      evidence_required: [deliv-comparison-table, deliv-paired-bootstrap, ref-cnn-baseline]
      automation: automated
    - id: test-metric-consistency
      subject: claim-rare-improvement
      kind: cross_method
      procedure: "Compute ViT macro-F1 using both sklearn.metrics.f1_score and torchmetrics.classification.MulticlassF1Score. Compare values."
      pass_condition: "Absolute difference between sklearn and torchmetrics macro-F1 < 1e-6 (same threshold as Phase 2)"
      evidence_required: [deliv-vit-metrics]
      automation: automated
    - id: test-paired-bootstrap-valid
      subject: claim-rare-improvement
      kind: consistency
      procedure: "Verify paired bootstrap implementation: (1) same indices used for ViT and CNN in each resample, (2) test set hash matches Phase 2 test manifest, (3) number of resamples >= 10,000, (4) rare-class definition matches Phase 2 (threshold=200, classes=Chirp,Wandering_Line,Helix,Light_Modulation)."
      pass_condition: "All 4 checks pass"
      evidence_required: [deliv-paired-bootstrap, ref-cnn-baseline]
      automation: automated
    - id: test-same-test-set
      subject: claim-rare-improvement
      kind: consistency
      procedure: "Hash the test_manifest.csv used for ViT evaluation and compare against the hash used in Phase 2. Both models must be evaluated on identical test samples."
      pass_condition: "SHA-256 hash of test_manifest.csv matches Phase 2 value"
      evidence_required: [deliv-vit-metrics, ref-cnn-baseline]
      automation: automated
  forbidden_proxies:
    - id: fp-overall-accuracy
      subject: claim-rare-improvement
      proxy: "Overall accuracy improvement without rare-class F1 improvement"
      reason: "The project claim is specifically about rare-class F1. A ViT that improves overall accuracy by reclassifying common classes better (e.g., Scattered_Light with 4158 test samples) while failing on rare classes (6-66 test samples) is false progress."
  links:
    - id: link-comparison
      source: claim-rare-improvement
      target: deliv-comparison-table
      relation: computes
      verified_by: [test-rare-f1-improvement]
    - id: link-visual
      source: claim-rare-improvement
      target: deliv-confusion-matrix
      relation: visualizes
      verified_by: [test-rare-f1-improvement]
    - id: link-statistical
      source: claim-rare-improvement
      target: deliv-paired-bootstrap
      relation: benchmarks
      verified_by: [test-paired-bootstrap-valid]
  uncertainty_markers:
    weakest_anchors:
      - "Wandering_Line has only 6 test samples; even perfect classification yields wide bootstrap CIs and limited statistical power for this individual class"
      - "Chirp has only 7 test samples; per-class F1 change is granular (each correct/incorrect prediction moves F1 by ~0.14)"
      - "Paired bootstrap on rare-class macro-F1 pools only 4 classes (93 total test samples); statistical power is inherently limited"
    disconfirming_observations:
      - "ViT rare-class macro-F1 <= CNN rare-class macro-F1 (0.3028) -- would trigger contract STOP condition"
      - "ViT rare-class macro-F1 > CNN but paired bootstrap p > 0.05 -- improvement exists but is not statistically significant; document honestly, may still be publishable with caveats"
      - "ViT overall macro-F1 substantially worse than CNN (< 0.60) -- would indicate training failure, not architecture limitation"
---

<objective>
Evaluate the trained ViT-B/16 on the held-out O3 temporal test set, perform paired statistical comparison against the CNN baseline, and produce all contract-required deliverables (comparison table, confusion matrices, statistical significance results).

Purpose: This is the decisive evaluation for the project's primary claim (claim-rare-improvement). The outputs directly populate deliv-comparison-table and deliv-confusion-matrix from the project contract.
Output: Per-class metrics, paired bootstrap results, comparison table, side-by-side confusion matrices, statistical summary with verdict.
</objective>

<execution_context>
@./.claude/get-physics-done/workflows/execute-plan.md
@./.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/03-vit-training-rare-class-optimization/03-01-SUMMARY.md
@.gpd/phases/02-cnn-baseline-reproduction/02-01-SUMMARY.md
@results/02-cnn-baseline/metrics.json
@results/02-cnn-baseline/per_class_f1.csv
@src/evaluation/evaluate.py
@src/evaluation/bootstrap_ci.py
@configs/vit_rare_class.yaml
@configs/cnn_baseline.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evaluate ViT and CNN on test set with paired bootstrap</name>
  <files>src/evaluation/paired_bootstrap.py, scripts/12_evaluate_vit.py, results/03-vit-rare-class/metrics.json, results/03-vit-rare-class/per_class_f1.csv, results/03-vit-rare-class/predictions.npz, results/03-vit-rare-class/paired_bootstrap_results.json</files>
  <action>
    Evaluate both models on the identical test set and run paired statistical comparison.

    **1. Paired bootstrap module (src/evaluation/paired_bootstrap.py):**
    - Implement `paired_bootstrap_rare_f1(y_true, y_pred_a, y_pred_b, rare_classes, n_resamples=10000, seed=42)`:
      - For each resample i in [0, n_resamples):
        - Draw bootstrap indices (same for both models): idx = rng.choice(N_test, size=N_test, replace=True)
        - Compute rare-class macro-F1 for model A: f1_a = macro_f1(y_true[idx], y_pred_a[idx], labels=rare_class_indices)
        - Compute rare-class macro-F1 for model B: f1_b = macro_f1(y_true[idx], y_pred_b[idx], labels=rare_class_indices)
        - Store difference: delta[i] = f1_a - f1_b
      - Point estimate: delta_hat = rare_macro_f1(y_true, y_pred_a) - rare_macro_f1(y_true, y_pred_b)
      - CI: [percentile(delta, 2.5), percentile(delta, 97.5)]
      - p-value (one-sided, H0: A <= B): fraction of resamples where delta <= 0
    - The rare-class macro-F1 computation MUST match Phase 2 exactly: sklearn.metrics.f1_score with average='macro', labels=rare_class_indices (not 'micro', not 'weighted')
    - Include unit test: synthetic data where model A is strictly better on rare classes -> p-value should be < 0.01

    **2. CNN re-evaluation (CRITICAL: per-sample predictions were not saved in Phase 2):**
    - Load CNN checkpoint from checkpoints/02-cnn-baseline/best_model.pt
    - Load test set using same GravitySpyDataset + get_eval_transforms()
    - Run inference to get per-sample predicted labels (argmax of logits)
    - Verify that recomputed CNN metrics EXACTLY match Phase 2 results:
      - macro-F1 must match 0.6786 to within 1e-4 (floating point from deterministic eval)
      - overall accuracy must match 0.9181 to within 1e-4
      - If metrics do NOT match, STOP and debug (test set mismatch or model loading error)
    - Save CNN predictions alongside ViT predictions for paired bootstrap

    **3. ViT evaluation:**
    - Load ViT checkpoint from checkpoints/03-vit-rare-class/best_model.pt
    - Load SAME test set (verify test_manifest.csv hash matches)
    - Run inference with eval_transforms (resize + normalize only, NO augmentation)
    - Compute per-class F1, per-class recall, per-class precision for all 23 classes
    - Compute macro-F1 and rare-class macro-F1 with bootstrap 95% CIs (10K resamples)
    - Cross-check: compute macro-F1 using BOTH sklearn and torchmetrics; difference must be < 1e-6

    **4. Paired bootstrap execution:**
    - Run paired_bootstrap_rare_f1(y_true, vit_preds, cnn_preds, rare_classes=['Chirp', 'Wandering_Line', 'Helix', 'Light_Modulation'], n_resamples=10000)
    - Record: point estimate, 95% CI, p-value
    - Also run paired bootstrap on overall macro-F1 for completeness (secondary metric)

    **5. Save all results:**
    - results/03-vit-rare-class/metrics.json: ViT metrics (same structure as Phase 2 metrics.json)
    - results/03-vit-rare-class/per_class_f1.csv: ViT per-class metrics (same columns as Phase 2)
    - results/03-vit-rare-class/predictions.npz: y_true, vit_preds, cnn_preds (for reproducibility)
    - results/03-vit-rare-class/paired_bootstrap_results.json: paired test results
    - Hash test_manifest.csv and record in metrics.json for traceability

    **Forbidden proxy enforcement:** All metric outputs must lead with rare-class macro-F1 as the primary result. Overall accuracy must be labeled "SANITY CHECK ONLY" wherever it appears.
  </action>
  <verify>
    1. CNN re-evaluation matches Phase 2: macro-F1 diff < 1e-4 from 0.6786, accuracy diff < 1e-4 from 0.9181
    2. Test set identity: SHA-256 hash of test_manifest.csv recorded and matches Phase 2
    3. Metric cross-check: sklearn vs torchmetrics ViT macro-F1 diff < 1e-6
    4. Bootstrap validity: n_resamples >= 10,000; paired (same indices for both models verified programmatically)
    5. Rare class definition: exactly 4 classes with threshold=200 (Chirp, Wandering_Line, Helix, Light_Modulation)
    6. predictions.npz contains y_true (48,845 entries), vit_preds (48,845), cnn_preds (48,845)
    7. Paired bootstrap unit test passes (synthetic data with known outcome)
  </verify>
  <done>ViT and CNN evaluated on identical test set, paired bootstrap completed with p-value reported, CNN re-evaluation matches Phase 2 metrics exactly, all results saved</done>
</task>

<task type="auto">
  <name>Task 2: Generate comparison deliverables and statistical summary</name>
  <files>results/03-vit-rare-class/comparison_table.csv, results/03-vit-rare-class/statistical_summary.md, figures/vit_confusion_matrix.png, figures/comparison_confusion_matrices.png, figures/comparison_per_class_f1.png</files>
  <action>
    Produce all contract-required comparison deliverables from Task 1 results.

    **1. Comparison table (deliv-comparison-table: results/03-vit-rare-class/comparison_table.csv):**
    - Columns: class, n_train, n_test, cnn_f1, cnn_f1_ci_lower, cnn_f1_ci_upper, vit_f1, vit_f1_ci_lower, vit_f1_ci_upper, f1_diff, cnn_recall, vit_recall, cnn_precision, vit_precision, is_rare
    - 23 rows (one per class), sorted by n_train ascending (rare classes first)
    - Add summary rows at bottom: "MACRO_ALL" (overall macro-F1), "MACRO_RARE" (rare-class macro-F1), "MACRO_COMMON" (common-class macro-F1)
    - f1_diff = vit_f1 - cnn_f1 (positive means ViT better)
    - CNN values loaded from results/02-cnn-baseline/per_class_f1.csv (must match exactly)

    **2. Side-by-side confusion matrices (deliv-confusion-matrix: figures/comparison_confusion_matrices.png):**
    - Two 23x23 confusion matrices side by side: ViT (left), CNN (right)
    - Row-normalized (show recall per class)
    - Annotate with raw counts
    - Highlight rare classes (Chirp, Wandering_Line, Helix, Light_Modulation) with colored border or background
    - Same class ordering in both matrices (sorted by n_train ascending)
    - Shared colorbar
    - Title: "ViT-B/16 vs CNN (ResNet-50) Confusion Matrices -- O3 Temporal Test Set (N=48,845)"
    - Also save standalone ViT confusion matrix: figures/vit_confusion_matrix.png

    **3. Per-class F1 comparison bar chart (figures/comparison_per_class_f1.png):**
    - Grouped bar chart: CNN (blue) vs ViT (orange) per-class F1 for all 23 classes
    - Error bars from bootstrap 95% CIs
    - Rare classes marked with asterisk or different hatch pattern
    - Horizontal reference lines for CNN and ViT rare-class macro-F1
    - Classes sorted by n_train (rare classes on left)

    **4. Statistical summary document (results/03-vit-rare-class/statistical_summary.md):**
    Write a structured summary containing:
    ```
    # ViT vs CNN Statistical Comparison -- Phase 3 Results

    ## Primary Result (Rare-Class Macro-F1)
    - CNN: {value} [{ci_lower}, {ci_upper}]
    - ViT: {value} [{ci_lower}, {ci_upper}]
    - Difference (ViT - CNN): {value} [{ci_lower}, {ci_upper}]
    - Paired bootstrap p-value: {value} (H0: ViT <= CNN)
    - Verdict: {SIGNIFICANT IMPROVEMENT / NOT SIGNIFICANT / ViT WORSE}

    ## Per-Class Rare Results
    - Chirp (11 train, 7 test): CNN F1={}, ViT F1={}
    - Wandering_Line (30 train, 6 test): CNN F1={}, ViT F1={}
    - Helix (33 train, 14 test): CNN F1={}, ViT F1={}
    - Light_Modulation (142 train, 66 test): CNN F1={}, ViT F1={}

    ## Secondary Results
    - Overall macro-F1: CNN={}, ViT={}
    - Overall accuracy (SANITY CHECK): CNN={}, ViT={}

    ## Contract Assessment
    - claim-rare-improvement: {PASS / FAIL / INCONCLUSIVE}
    - test-rare-f1-improvement: {PASS (p < 0.05) / FAIL}
    - fp-overall-accuracy enforcement: {CONFIRMED -- primary metric is rare-class F1}
    - Backtracking trigger: {NOT TRIGGERED / TRIGGERED -- ViT does not beat CNN}
    ```

    **5. Backtracking trigger assessment:**
    - If ViT rare-class macro-F1 <= CNN rare-class macro-F1 (0.3028): document as STOP condition per contract
    - If ViT > CNN but p >= 0.05: document as inconclusive, note limited statistical power from small rare-class test sets
    - If ViT > CNN and p < 0.05: PASS -- claim-rare-improvement is supported

    **Forbidden proxy final check:** Review ALL outputs (metrics.json, comparison_table.csv, statistical_summary.md, figure titles). Confirm that rare-class macro-F1 is presented as primary, overall accuracy is labeled SANITY CHECK everywhere.
  </action>
  <verify>
    1. comparison_table.csv has 23 data rows + 3 summary rows, all required columns present, no NaN values
    2. CNN column in comparison_table.csv matches Phase 2 per_class_f1.csv values exactly
    3. Confusion matrices are 23x23, row-normalized, rare classes visually highlighted
    4. Statistical summary contains all required fields: both model metrics, difference with CI, p-value, verdict, contract assessment
    5. Backtracking trigger correctly assessed based on results
    6. Forbidden proxy enforcement: grep all output files for "accuracy" -- every occurrence must be accompanied by "SANITY CHECK" or equivalent qualifier
    7. Figures render correctly (not blank, reasonable color scales, legends present)
  </verify>
  <done>Comparison table with per-class F1/recall for all 23 classes produced, side-by-side confusion matrices with rare-class highlighting generated, statistical summary with paired bootstrap verdict written, backtracking trigger assessed, forbidden proxy enforcement verified across all outputs</done>
</task>

</tasks>

<verification>
- CNN re-evaluation reproduces Phase 2 metrics exactly (macro-F1 = 0.6786, accuracy = 0.9181)
- ViT and CNN evaluated on identical test set (manifest hash match)
- Paired bootstrap uses same indices for both models in each resample
- Metric cross-check: sklearn vs torchmetrics < 1e-6
- Comparison table contains all contract-required fields (per-class F1, recall, sample counts, significance)
- Confusion matrices are side-by-side with rare-class highlighting
- Statistical summary contains clear verdict and contract assessment
- Forbidden proxy (fp-overall-accuracy) enforced in all outputs
- Backtracking trigger assessed against contract criteria
</verification>

<success_criteria>
ViT evaluated on identical test set as CNN, paired bootstrap p-value computed for rare-class macro-F1 difference, deliv-comparison-table and deliv-confusion-matrix produced with all contract-required contents, statistical summary written with clear verdict and backtracking trigger assessment. If ViT rare-class F1 > CNN AND p < 0.05: claim-rare-improvement PASSES. If not: backtracking trigger documented per contract.
</success_criteria>

<output>
After completion, create `.gpd/phases/03-vit-training-rare-class-optimization/03-02-SUMMARY.md`
</output>
